# author: Lukas Kluft
# version: 29.10.2015
#
# purpose: pull all git repositories

blue='\e[1;34m'
white='\e[0m'

for gitdir in $HOME/git/*
    do
    if [[ "$gitdir" != *".git" ]];then
        cd $gitdir
        echo -e Aktuelles Verzeichnis: $blue$(pwd)$white
        git pull
    fi
    done
